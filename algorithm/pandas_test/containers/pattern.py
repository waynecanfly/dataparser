# -*- coding: UTF-8 -*-

class Pattern:
    def __init__(self, id, lineNum, text, identity):
        self.id = id
        self.lineNum = lineNum
        self.text = text
        self.identity = identity
